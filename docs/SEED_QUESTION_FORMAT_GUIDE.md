# 種子試題 JSON 格式指南

## 📋 基本結構

種子試題 JSON 是一個**數組**，每個元素代表一道題目：

```json
[
  {
    "question": "題目文字",
    "answer": "答案",
    "topic": "單元名稱",
    "explanation": "解釋（選填）",
    "hint": "提示（選填）"
  }
]
```

## 🎨 添加圖形字段的位置

**在每個題目對象中添加 `shape` 和 `params` 字段**：

```json
[
  {
    "question": "計算這個長方形的面積。",
    "answer": "15",
    "topic": "面積",
    "shape": "rectangle",           // ← 在這裡添加圖形類型
    "params": {                      // ← 在這裡添加圖形參數
      "w": 5,
      "h": 3
    }
  }
]
```

---

## 📐 各類圖形的完整示例

### 1. 長方形 (Rectangle)

```json
{
  "question": "計算這個長方形的面積。長 $5$cm，寬 $3$cm。",
  "answer": "15",
  "topic": "面積",
  "type": "geometry",
  "shape": "rectangle",
  "params": {
    "w": 5,
    "h": 3
  },
  "explanation": "長方形面積 = 長 × 寬 = 5 × 3 = 15",
  "hint": "記住長方形面積公式"
}
```

**參數說明：**
- `w`: 寬度（單位：cm）
- `h`: 高度（單位：cm）

---

### 2. 正方形 (Square)

```json
{
  "question": "計算這個正方形的周界。邊長是 $8$cm。",
  "answer": "32",
  "topic": "周界",
  "type": "geometry",
  "shape": "square",
  "params": {
    "s": 8
  },
  "explanation": "正方形周界 = 邊長 × 4 = 8 × 4 = 32"
}
```

**參數說明：**
- `s`: 邊長（單位：cm）

---

### 3. 三角形 (Triangle)

```json
{
  "question": "計算這個三角形的面積。底邊長 $6$cm，高 $4$cm。",
  "answer": "12",
  "topic": "面積",
  "type": "geometry",
  "shape": "triangle",
  "params": {
    "base": 6,
    "height": 4
  },
  "explanation": "三角形面積 = 底 × 高 ÷ 2 = 6 × 4 ÷ 2 = 12"
}
```

**參數說明：**
- `base` 或 `b`: 底邊長度（單位：cm）
- `height` 或 `h`: 高度（單位：cm）

---

### 4. 圓形 (Circle)

```json
{
  "question": "計算這個圓的周界。半徑是 $5$cm。",
  "answer": "31.4",
  "topic": "周界",
  "type": "geometry",
  "shape": "circle",
  "params": {
    "radius": 5
  },
  "explanation": "圓周界 = 2 × π × 半徑 = 2 × 3.14 × 5 = 31.4"
}
```

**參數說明：**
- `radius` 或 `r`: 半徑（單位：cm）

---

### 5. 梯形 (Trapezoid)

```json
{
  "question": "計算這個梯形的面積。上底 $4$cm，下底 $8$cm，高 $5$cm。",
  "answer": "30",
  "topic": "面積",
  "type": "geometry",
  "shape": "trapezoid",
  "params": {
    "top": 4,
    "bottom": 8,
    "height": 5
  },
  "explanation": "梯形面積 = (上底 + 下底) × 高 ÷ 2 = (4 + 8) × 5 ÷ 2 = 30"
}
```

**參數說明：**
- `top`: 上底長度（單位：cm）
- `bottom` 或 `base`: 下底長度（單位：cm）
- `height` 或 `h`: 高度（單位：cm）

---

### 6. 平行四邊形 (Parallelogram)

```json
{
  "question": "計算這個平行四邊形的面積。底邊 $8$cm，高 $4$cm。",
  "answer": "32",
  "topic": "面積",
  "type": "geometry",
  "shape": "parallelogram",
  "params": {
    "base": 8,
    "height": 4
  },
  "explanation": "平行四邊形面積 = 底 × 高 = 8 × 4 = 32"
}
```

**參數說明：**
- `base` 或 `b`: 底邊長度（單位：cm）
- `height` 或 `h`: 高度（單位：cm）

---

### 7. 不規則圖形 (Irregular Polygon)

```json
{
  "question": "計算這個不規則圖形的周界。",
  "answer": "24",
  "topic": "周界",
  "type": "geometry",
  "shape": "irregular",
  "params": {
    "points": [
      { "x": -2, "y": -1 },
      { "x": 2, "y": -1 },
      { "x": 3, "y": 2 },
      { "x": -1, "y": 2 }
    ]
  },
  "explanation": "將各邊長度相加得到周界"
}
```

**參數說明：**
- `points`: 點的數組，每個點有 `x` 和 `y` 座標（相對座標，單位：cm）

---

### 8. 複合圖形 (Composite Shape)

```json
{
  "question": "計算這個複合圖形的面積。",
  "answer": "28",
  "topic": "面積",
  "type": "geometry",
  "shape": "composite",
  "params": {
    "shapes": [
      {
        "type": "rectangle",
        "params": { "w": 4, "h": 3 },
        "offset": { "x": -2, "y": -1 }
      },
      {
        "type": "square",
        "params": { "s": 4 },
        "offset": { "x": 2, "y": 1 }
      }
    ]
  },
  "explanation": "將兩個圖形的面積相加"
}
```

**參數說明：**
- `shapes`: 子圖形數組
  - `type`: 子圖形類型（rectangle, square 等）
  - `params`: 子圖形參數
  - `offset`: 偏移位置 `{x, y}`（單位：cm）

---

### 9. 8 位方向地圖 (Map Grid)

```json
{
  "question": "從起點向北走 2 格，再向東走 3 格，你會到達哪裡？",
  "answer": "公園",
  "topic": "方向",
  "type": "geometry",
  "shape": "map_grid",
  "mapData": {
    "gridSize": {
      "rows": 5,
      "cols": 5
    },
    "startPos": {
      "row": 2,
      "col": 2
    },
    "path": [
      {
        "direction": "north",
        "steps": 2
      },
      {
        "direction": "east",
        "steps": 3
      }
    ],
    "landmarks": [
      {
        "row": 1,
        "col": 1,
        "label": "學校"
      },
      {
        "row": 4,
        "col": 4,
        "label": "公園"
      }
    ]
  },
  "explanation": "從 (2,2) 向北 2 格到 (0,2)，再向東 3 格到 (0,5)，即公園位置"
}
```

**參數說明：**
- `gridSize`: 網格大小 `{rows, cols}`
- `startPos`: 起點位置 `{row, col}`（從 0 開始）
- `path`: 路徑數組
  - `direction`: 方向（north, south, east, west, northeast, northwest, southeast, southwest）
  - `steps`: 步數
- `landmarks`: 地標數組 `[{row, col, label}]`

---

## 📝 完整示例：混合題目

```json
[
  {
    "question": "計算這個長方形的面積。長 $5$cm，寬 $3$cm。",
    "answer": "15",
    "topic": "面積",
    "type": "geometry",
    "shape": "rectangle",
    "params": {
      "w": 5,
      "h": 3
    },
    "explanation": "長方形面積 = 長 × 寬 = 5 × 3 = 15"
  },
  {
    "question": "計算這個三角形的面積。底邊長 $6$cm，高 $4$cm。",
    "answer": "12",
    "topic": "面積",
    "type": "geometry",
    "shape": "triangle",
    "params": {
      "base": 6,
      "height": 4
    },
    "explanation": "三角形面積 = 底 × 高 ÷ 2 = 6 × 4 ÷ 2 = 12"
  },
  {
    "question": "有 45 粒糖果，平均分給 7 位小朋友，每人可得幾粒？還剩下幾粒？",
    "answer": "6...3",
    "topic": "除法",
    "type": "word_problem"
  }
]
```

---

## 🔧 操作步驟

### 步驟 1：準備 JSON 文件

1. 打開文本編輯器（如 Notepad++, VS Code）
2. 創建一個新文件，保存為 `.json` 格式（例如：`seed_questions.json`）

### 步驟 2：添加題目

對於**有圖形的題目**，添加 `shape` 和 `params`：

```json
{
  "question": "你的題目文字",
  "answer": "答案",
  "topic": "單元名稱",
  "shape": "圖形類型",      // ← 添加這行
  "params": {               // ← 添加這行和參數
    "參數名": 數值
  }
}
```

對於**沒有圖形的題目**，保持原樣：

```json
{
  "question": "你的題目文字",
  "answer": "答案",
  "topic": "單元名稱"
}
```

### 步驟 3：驗證 JSON 格式

使用在線 JSON 驗證工具（如 https://jsonlint.com/）檢查格式是否正確。

### 步驟 4：上傳到系統

1. 登入系統，進入「開發者後台」
2. 選擇「試卷庫 & 種子管理」標籤
3. 選擇年級和單元（可選）
4. 將 JSON 內容複製貼上到文本框
5. 點擊「批量上傳種子」

---

## ⚠️ 注意事項

1. **JSON 格式必須正確**：確保所有引號、逗號、括號都正確配對
2. **數組格式**：整個文件必須是數組格式 `[...]`
3. **單位統一**：所有長度單位建議使用 `cm`
4. **LaTeX 公式**：數學公式使用 `$\frac{3}{8}$` 格式
5. **可選字段**：`explanation`、`hint`、`type` 是可選的

---

## 📚 快速參考表

| 圖形類型 | shape 值 | params 格式 |
|---------|---------|------------|
| 長方形 | `"rectangle"` | `{"w": 5, "h": 3}` |
| 正方形 | `"square"` | `{"s": 8}` |
| 三角形 | `"triangle"` | `{"base": 6, "height": 4}` |
| 圓形 | `"circle"` | `{"radius": 5}` |
| 梯形 | `"trapezoid"` | `{"top": 4, "bottom": 8, "height": 5}` |
| 平行四邊形 | `"parallelogram"` | `{"base": 8, "height": 4}` |
| 不規則圖形 | `"irregular"` | `{"points": [{x, y}, ...]}` |
| 複合圖形 | `"composite"` | `{"shapes": [{type, params, offset}, ...]}` |
| 地圖網格 | `"map_grid"` | `{"mapData": {...}}` |

---

## 💡 提示

- 如果不確定圖形類型，可以先上傳只有文字的題目，系統會自動生成
- 圖形參數可以從 PDF 中的圖形標註中提取
- 建議先測試幾道題目，確認格式正確後再批量上傳
